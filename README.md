# Learning LlamaIndex TS

## Running the Project

1. Clone the repository

1. Install dependencies
    ```bash
    yarn install
    ```

1. Run Qrant with Docker:
    ```bash
    docker-compose up
    ``` 
